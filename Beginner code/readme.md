Created a encoder/decoder GUI that essentially translates each letter in the word by the same random number, 
and encodes the word. To decode, I used a word database and match it to every possible sequence (numbers 1-25)
to find if the word exist, if it does, it will appear in the text box. 
